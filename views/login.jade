doctype
html
    head
        meta(charset="utf-8")
        title login
    body
        form.loginForm(action="/process_post" method="POST" )
            input.loginInput.loginuser(type="text", name="name" placeholder="#{json.name}")
            input.loginInput(type="password",name="age" placeholder="密码")
            p.loginerr #{loginerrMessage}
            input.submit(type="submit",value="添加")